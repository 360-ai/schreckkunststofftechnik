{
  "name": "schreck-kunststofftechnik-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "deploy": "npm run build && wrangler pages deploy dist --project-name schreck-kunststofftechnik"
  },
  "dependencies": {
    "astro": "^5.7.0",
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
