# Cloudflare Pages Konfiguration (Deployment erst nach Freigabe durch Denis).
# Lokal testen: npx wrangler pages dev dist
name = "schreck-fable"
compatibility_date = "2026-07-01"
pages_build_output_dir = "dist"
